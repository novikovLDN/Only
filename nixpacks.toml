[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "alembic upgrade head && python -m app.main & uvicorn app.health:app --host 0.0.0.0 --port ${PORT:-8080}"
